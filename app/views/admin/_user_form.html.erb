<%= form_with model: user, url: path, class: "contents" do |form| %>
  <% is_new = user.new_record? %>
  <% if user.errors.any? %>
    <div class="alert alert-danger bg-red-50 border border-red-200 text-red-600 px-4 py-3 rounded mb-5">
      <h4 class="font-medium"><%= pluralize(user.errors.count, t('error')) %> <%= t('auth.prohibited_from_being_saved') %></h4>
      <ul class="list-disc list-inside mt-2">
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="my-5">
    <%= form.label :username, t('auth.username'), class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_field :username, required: true, autofocus: true, placeholder: t('auth.enter_username'), class: "block shadow-sm rounded-md border border-gray-400 focus:outline-blue-600 px-3 py-2 mt-2 w-full" %>
  </div>
  <div class="my-5">
    <%= form.label :email_address, t('auth.email_address'), class: "block text-sm font-medium text-gray-700" %>
    <%= form.email_field :email_address, required: true, autocomplete: "username", placeholder: t('auth.enter_email'), class: "block shadow-sm rounded-md border border-gray-400 focus:outline-blue-600 px-3 py-2 mt-2 w-full" %>
  </div>
  <% if is_new %>
    <div class="my-5">
      <%= form.label :password, t('auth.password'), class: "block text-sm font-medium text-gray-700" %>
      <%= form.password_field :password, required: true, autocomplete: "new-password", placeholder: t('auth.enter_password'), minlength: 6, maxlength: 72, class: "block shadow-sm rounded-md border border-gray-400 focus:outline-blue-600 px-3 py-2 mt-2 w-full" %>
      <p class="text-sm text-gray-500 mt-1"><%= t('auth.password_requirements') %></p>
    </div>
    <div class="my-5">
      <%= form.label :password_confirmation, t('auth.password_confirmation'), class: "block text-sm font-medium text-gray-700" %>
      <%= form.password_field :password_confirmation, required: true, autocomplete: "new-password", placeholder: t('auth.confirm_password'), minlength: 6, maxlength: 72, class: "block shadow-sm rounded-md border border-gray-400 focus:outline-blue-600 px-3 py-2 mt-2 w-full" %>
    </div>
  <% end %>
  <div class="col-span-6 sm:flex sm:items-center sm:gap-4">
    <div class="inline">
      <%= form.submit submit_btn, class: "w-full sm:w-auto text-center rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
    </div>
  </div>
<% end %>
