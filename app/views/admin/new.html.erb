<div class="mx-auto md:w-2/3 w-full">
  <% if flash[:alert] %>
    <p class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg inline-block" id="alert"><%= flash[:alert] %></p>
  <% end %>
  <% if flash[:notice] %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= flash[:notice] %></p>
  <% end %>
  <h1 class="font-bold text-4xl"><%= t('admin.dashboard.new_user') %></h1>
  <%= render 'user_form', user: @user, path:  admin_create_user_path, submit_btn: t('auth.sign_up') %>
</div>
