<% content_for :title, "Tasks" %>
<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Tasks</h1>
    <%= link_to "New task", new_task_path, class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white block font-medium" %>
  </div>
  <div id="tasks" class="min-w-full divide-y divide-gray-200 space-y-5">
    <% if @tasks.any? %>
      <% @tasks.each do |task| %>
        <div class="bg-white p-6 rounded-lg shadow-sm border border-gray-200 hover:shadow-md transition-shadow">
          <div class="flex justify-between items-start mb-4">
            <div class="flex-1">
              <h3 class="text-lg font-semibold text-gray-900 mb-2">
                <%= link_to task.title, task, class: "hover:text-blue-600" %>
              </h3>
              <% if task.content.present? %>
                <p class="text-gray-600 text-sm mb-3" style="display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
                  <%= truncate(task.content, length: 120) %>
                </p>
              <% end %>
              <!-- Badges for priority and status -->
              <div class="flex flex-wrap gap-2 mb-3">
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium">
                  <%= task.priority %>
                </span>
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium">
                  <%= task.status.humanize %>
                </span>
              </div>
              <!-- Time info -->
              <% if task.start_time.present? || task.end_time.present? %>
                <div class="bg-gray-50 p-3 rounded-md mb-3">
                  <div class="flex items-center text-xs text-gray-600 space-x-4">
                    <% if task.start_time.present? %>
                      <div class="flex items-center">
                        <span class="font-medium">Start Time:</span>
                        <span class="ml-1"><%= task.start_time.strftime("%d/%m/%Y %H:%M") %></span>
                      </div>
                    <% end %>
                    <% if task.end_time.present? %>
                      <div class="flex items-center">
                        <span class="font-medium">End Time:</span>
                        <span class="ml-1"><%= task.end_time.strftime("%d/%m/%Y %H:%M") %></span>
                      </div>
                    <% end %>
                    <% if task.start_time.present? && task.end_time.present? %>
                      <div class="flex items-center text-blue-600">
                        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                        <span class="font-medium">Duration:</span>
                        <span class="ml-1">
                          <% duration = ((task.end_time - task.start_time) / 1.hour).round(1) %>
                          <%= duration == duration.to_i ? duration.to_i : duration %> hours
                        </span>
                      </div>
                    <% end %>
                  </div>
                </div>
              <% end %>
            </div>
            <!-- Action Buttons -->
            <div class="flex flex-col gap-2 ml-4">
              <%= link_to "Edit", edit_task_path(task), class: "text-center rounded-md px-3 py-1.5 bg-blue-100 hover:bg-blue-200 text-blue-700 text-sm font-medium touch-target no-select" %>
              <%= button_to "Delete", task, method: :delete, class: "rounded-md px-3 py-1.5 text-white bg-red-600 hover:bg-red-700 text-sm font-medium touch-target no-select", data: { turbo_confirm: "Are you sure?" } %>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="text-center py-12">
        <p class="text-gray-500 text-lg mb-4">No tasks found.</p>
        <%= link_to "Create your first task", new_task_path, class: "rounded-md px-4 py-2 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
      </div>
    <% end %>
  </div>
</div>
